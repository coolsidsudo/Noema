# Packages and Services

This directory is reserved for future Noema packages and service implementations.

Examples of future additions:
- API services
- maintainer/compiler workflows
- CLI tools
- shared libraries

Do not over-design package boundaries in Phase 1; use this as the landing zone for future slices.
